version: '3.8'

services:
  k-story:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: k-story
    ports:
      - "3000:3000"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: '0.5'
